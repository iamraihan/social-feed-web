# Backend API base URL (server-only — never exposed to the browser).
# Local dev: backend runs on http://localhost:8000 (PORT in api repo).
API_URL=http://localhost:8000

# Set to "true" in production so session cookies require HTTPS.
COOKIE_SECURE=false
